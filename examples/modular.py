import sub.functions as functions


def main():
    x = 6
    print(f'Area of circle with radius {x}:', functions.area(x))
    print(f'Factorial of {x}:', functions.factorial(x))


if __name__=='__main__':
    main()
